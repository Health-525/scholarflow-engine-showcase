name: 处理调课

# Status: showcase-only
# 说明：公开版仅展示调课解析与课表更新边界，不公开完整私有实现。

on:
  repository_dispatch:
    types: [adjustments-updated]
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: 说明
        run: |
          echo "展示版工作流：解析调课记录，更新课表数据，并触发重新生成课表产物"