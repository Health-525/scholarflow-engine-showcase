name: 处理运动记录

# Status: showcase-only
# 说明：公开版仅展示运动记录聚合职责，不公开完整私有实现。

on:
  repository_dispatch:
    types: [running-updated]
  workflow_dispatch:

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: 说明
        run: |
          echo "展示版工作流：从复选框记录生成统计结果与可视化数据"